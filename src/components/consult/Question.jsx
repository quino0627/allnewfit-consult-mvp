import React from 'react';
import styled from 'styled-components';

const QuestionBlock = styled.div`
  margin-top: 30px;
`;
const Question = () => {
  return <QuestionBlock>asdf</QuestionBlock>;
};

export default Question;
